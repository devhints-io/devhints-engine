import React from 'react'
import CSS from './PreFooter.module.css'

/**
 * Pre-footer separator in the cheatsheets page
 */

export const PreFooter = () => (
  <div className={CSS.root}>
    <i className={CSS.icon} />
  </div>
)

export default PreFooter
